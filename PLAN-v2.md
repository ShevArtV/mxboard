# mxBoard v2 — план: иерархия и формализация задач

> Статус: согласовано, к реализации. Составлен 2026-07-15.
> Продолжает работающий 1.0.0-alpha (доска + REST + MCP). Данные на стенде выкидные —
> схема пересобирается целиком, миграций нет.

## Что добавляет v2

Разворот из «одна доска» в **отдел → проект → задача**, с типизацией задач:
у каждого типа задачи свой набор обязательных полей (багфикс: где сломалось, что,
как воспроизвести, как должно быть). Плюс подзадачи (привлечение соисполнителей),
видимость по вовлечённости, дедлайны с оспариванием, фильтры, и агент-менеджер,
который через MCP создаёт задачи, типы и проекты.

---

## Принятые решения (и почему)

1. **Отдел = группа пользователей MODX**, но не любая. `Customers`, `Subscribers` —
   тоже группы, но не отделы. Поэтому тонкий реестр `mxboard_department` помечает,
   какие группы являются отделами. Членство, роли, супер-пользователь — всё из MODX.
2. **Проект = доска.** Отдельной сущности «доска» нет; колонки принадлежат проекту.
3. **Тип задачи обязателен.** Задач без типа нет — простая задача с одним заголовком
   ничем не лучше типизированной, а формализация и есть цель.
4. **Тип обязан иметь ≥1 своё поле.** Тип только со встроенными полями бессмыслен.
5. **Встроенные поля задачи** (всегда есть, не описываются в типе): `title` (≤250,
   обязателен), `deadline` (обязателен), `type`. `mxboard_field` описывает только
   ДОПОЛНИТЕЛЬНЫЙ контент.
6. **Значения полей — JSON** на задаче (это формализованный контент). Метаданные
   (стадия, автор, исполнитель, дедлайн) — настоящие колонки, по ним фильтруем/сортируем.
7. **Стадии — проектные.** Ключ `done` не захардкожен: правило закрытия завязано на
   флаг `is_final`. Инвариант: у проекта ровно одна начальная и ровно одна финальная.
   `stage_key` (необязательный сквозной тег) даёт отчётность через проекты — так стадия
   становится «общей».
8. **Подзадачи вместо ревьюеров** (роль ревьюера сочли слишком узкой; наблюдателей
   тоже нет). Привлечь кого-то к работе = завести **подзадачу**: это обычная задача
   с `parent_id`, живёт в `mxboard_task` (отдельной таблицы нет). Создать подзадачу
   может **и автор, и исполнитель** основной. Незавершённая (не в финальной стадии)
   подзадача — **блокер**: основную нельзя перевести в финальную, пока открыт хоть
   один потомок.
9. **Видимость**: вижу задачу, если я автор / исполнитель, либо автор / исполнитель
   любой её подзадачи (это даёт чтение + комментирование основной), либо
   супер-пользователь группы её проекта, либо глобальный sudo. Основная при этом
   **не попадает в канбан** соисполнителя подзадачи — у него в доске только сама
   подзадача (его задача).
10. **Дедлайн оспаривается, не меняется исполнителем.** Исполнитель предлагает новую
    дату с причиной; автор принимает (дедлайн меняется) или отклоняет.
11. **MCP: `tools/list` фильтруется по правам токена.** Исполнитель видит ~6 тулов,
    менеджер — весь набор. Тулы дискретные (лучшая читаемость/валидация), но каждый
    агент несёт только свой срез — контекст не раздувается.
12. **Файлы**: значение поля-файла в JSON (`path`, `name`, `size`); заливка в UI через
    медиа-источник MODX (`mxboard.media_source`); агент через MCP пока задаёт URL.
13. **Исполнитель назначается автором ПРИ СОЗДАНИИ** (обновление модели, отменяет старый
    «свободный пул + захват»). Обязателен и **обязан быть членом отдела проекта** (автор —
    кто угодно, кросс-отдел: «программист ставит задачу дизайнеру»). Свободного пула нет →
    **атомарный захват `task_take` удалён** (гонки за карточку больше нет — карточка сразу
    именная). Переназначение — правкой задачи (автор/менеджер), новый исполнитель тоже из
    отдела. Флаг колонки `is_ready` (откуда «брали») удалён: у стадий значимы только
    `is_initial` и `is_final`.
14. **Доска — без визуального шума**: на канбане вижу только карточки, где я автор или
    исполнитель (менеджер — все). Подзадачи, где я ни при чём, на доску НЕ выводятся; в
    связанную задачу перехожу из родителя (деталь по `canView` даёт чтение/комментарий).
15. **Уведомления + непрочитанные** (подфаза 3e). Системные события уже шлёт `TaskService`
    (create/move/close/comment/update/delete/dispute/resolve) — добавляется `mxbOnTaskAssign`
    (назначение при создании и переназначении). Бандл-плагин `mxBoardNotify` на этих событиях
    пишет уведомления. Получатели: назначена → исполнителю; смена стадии → второй стороне;
    выполнена → автору; новый комментарий → автору+исполнителю+участникам подзадач (кроме
    инициатора). Счётчик непрочитанных комментариев на карточке — таблица `mxboard_task_seen`
    (`user_id`, `task_id`, `seenon`); badge = комментарии с `createdon > seenon`; открытие
    страницы задачи сбрасывает `seenon`. Хранилище — таблица `mxboard_notification`
    (`user_id`, `task_id`, `type`, `read`, `createdon`). **Доставка — через `universaleventbus`**
    (пакет пользователя): mxBoard кладёт сообщение в очередь реестра `branch = mxb_user_<id>`
    (`QueueManager::addToQueue`, напрямую — не через ecom-логику `EventBus::handleEvent`), фронт
    подписан через `eventbus.js` (+ `broadcastchannel.js` шарит одно SSE-соединение между
    вкладками → снимает риск воркеров). **ЗАВИСИМОСТЬ:** `universaleventbus` сейчас **MODX 2**,
    не портирован → 3e ждёт отдельную задачу «порт universaleventbus на MODX 3». Порт должен
    закрыть: mgr-контекст SSE (`initialize('mgr')`, распознать менеджерского получателя как
    branch) и `session_write_close()` до цикла (иначе лок файловой сессии вешает весь менеджер;
    стоковый `ssehandler.php` пишет `$_SESSION` в цикле — в админке это баг).

---

## Права исполнителя (жёстко)

| Действие | Исполнитель | Автор / супер-группы / sudo |
|---|---|---|
| Двигать до финальной (не в неё) | да | да |
| Перевести в финальную (закрыть) | нет | да (если нет открытых подзадач) |
| Создать подзадачу | да | да |
| Комментировать | да | да |
| Оспорить дедлайн | да | — |
| Принять/отклонить оспаривание | нет | да |
| Править поля / тип / дедлайн / приоритет | нет | да |
| Удалить задачу | нет | да |

«Менеджер» = супер-пользователь группы отдела или глобальный sudo (тот же признак,
что в видимости).

---

## Модель данных (9 таблиц)

- **mxboard_department** — `usergroup_id` (группа MODX), `name` (переопределение), `active`, `position`, `createdon`. Реестр: помечает группу как отдел.
- **mxboard_project** — `department_id`, `key`, `name`, `description`, `active`, `position`, `createdon`, `updatedon`. Уникум `(department_id, key)`.
- **mxboard_column** — `project_id`, `key`, `name`, `position`, `move_roles`, `stage_key` (nullable), `is_initial`, `is_final`, `createdon`. `project_id=0` — глобальный шаблон. Уникум `(project_id, key)`. (`is_ready` убран: свободного пула нет.)
- **mxboard_task_type** — `department_id`, `key`, `name`, `description`, `active`, `position`, `createdon`. «Рабочий» только при ≥1 поле. Уникум `(department_id, key)`.
- **mxboard_field** — `task_type_id`, `key`, `label`, `type` (text/textarea/url/number/date/select/user/file), `required`, `position`, `options` (JSON), `createdon`. Уникум `(task_type_id, key)`.
- **mxboard_task** — метаданные: `project_id`, `parent_id` (nullable — если задан, задача является подзадачей; тот же проект, что у родителя), `type_id`, `column_id`, `author_id`, `assignee_id`, `priority`, `position`, `deadlineon`, `deadline_disputed`, `deadline_proposed`, `createdon`, `updatedon`, `startedon`, `closedon`; контент: `title` (≤250), `tor` (опц.), `fields` (JSON), `meta` (JSON). Индекс по `parent_id`.
- **mxboard_comment** — `task_id`, `user_id`, `content`, `createdon`. Без изменений.
- **mxboard_log** — `task_id`, `user_id`, `action`, `from_column`, `to_column`, `note`, `channel`, `createdon`. Новые action: `deadline_dispute`, `deadline_accepted`, `deadline_rejected`, `subtask_add`.
- **mxboard_token** — `user_id`, `name`, `token_hash` (sha256), `active`, `lastusedon`, `createdon`. Без изменений.

---

## Инварианты (проверяются на сервере)

- Проект: **ровно одна** `is_initial`, **ровно одна** `is_final`; ≥1 `is_ready` — предупреждение.
- Тип: **≥1** поле, иначе не «рабочий» (не в `task_schema`/`task_create`).
- Задача: `type_id` обязателен и валиден; `title` непустой ≤250; `deadlineon` > 0.
- В финальную стадию (по флагу `is_final`) — только автор/супер/sudo.
- В финальную **нельзя, пока есть незавершённая подзадача** (потомок не в своей финальной стадии) — блокер.
- Самозакрытие (автор=исполнитель) запрещено, если `mxboard.allow_self_close=0`.
- Подзадача (`parent_id` задан) наследует проект родителя; `parent_id` должен ссылаться на существующую задачу того же проекта.

---

## Механика оспаривания дедлайна

- Исполнитель: `task_dispute_deadline(task_id, proposed_date, reason)` → `deadline_disputed=1`,
  `deadline_proposed=date`, причина в журнал (`deadline_dispute`), событие.
- Автор: `task_resolve_dispute(task_id, accept)` →
  - accept: `deadlineon=deadline_proposed`, флаг и proposed сброшены, журнал `deadline_accepted`;
  - reject: флаг и proposed сброшены, дедлайн прежний, журнал `deadline_rejected`.

---

## MCP — дискретные тулы, `tools/list` по правам токена

**Чтение** (кто видит доску): `board_list`, `task_get` (отдаёт родителя и список подзадач), `task_schema`, `project_list`, `department_users` (кого можно назначить исполнителем — члены отдела проекта).
**Жизненный цикл**: `task_create` (создавший=автор; **`assignee` обязателен, член отдела проекта**; опц. `parent_id` → подзадача, разрешено только автору/исполнителю родителя), `task_move` (блок финальной при открытых подзадачах), `task_comment`, `task_dispute_deadline`.
**Авторские** (автор/супер/sudo): `task_update` (в т.ч. переназначение — новый исполнитель из отдела), `task_resolve_dispute`, `task_delete`.
**Структура** (супер/sudo — агент-менеджер): `type_create`/`type_update`, `project_create`/`project_update`, `department_register`.

- `type_create` принимает поля в том же вызове (инвариант ≥1 поле).
- `project_create` принимает колонки (инвариант одна initial / одна final) либо берёт шаблон.
- `task_schema` отдаёт полный обязательный набор: встроенные (title, deadline) + обязательные поля типа.

---

## REST — второй агентский канал (тот же сервис, что MCP)

Отдельный HTTP REST для пользователей и внешних интеграций — параллельно MCP, поверх того же
`TaskService` (единый источник правил и журнала). Точка входа `assets/components/mxboard/rest.php`:
бутстрап MODX как в `mcp.php` (живая сессия ради корректных политик, куки не отдаём),
гейт настройкой `mxboard.api_enabled`.

**Авторизация — оба метода на обоих каналах.** Единый резолвер `ApiAuth`: сначала
`Authorization: Bearer <token>` (`TokenAuth`, как раньше), иначе `Authorization: Basic`
логин+пароль MODX (`PasswordAuth` через `modUser::passwordMatches`). И MCP, и REST принимают
любой из двух — клиент выбирает сам. Дальше канал действует от имени этого пользователя,
права считаются для него (sudo-флаг + authority группы работают без реальной сессии, обходя
ловушку `hasPermission`). Пароль в конфиге агента — менее безопасно, чем отзываемый токен;
это выбор пользователя, оба варианта поддержаны.

Маршрутизация по `PATH_INFO` с фолбэком на `?path=` (shared-хостинг Hostland не всегда
отдаёт PATH_INFO). Ответы — JSON `{success, message, data}`, коды HTTP по смыслу
(200/201/400/401/403/404). Права — внутри `TaskService`, канал `channel='api'` в журнале.

| Метод + путь | Действие |
|---|---|
| `GET /projects` | список видимых проектов |
| `GET /board?project=key` | колонки + видимые карточки проекта |
| `GET /tasks/{id}` | карточка (родитель + подзадачи), если `canView` |
| `GET /types/{key}/schema?project=key` | схема типа (встроенные + обязательные поля) |
| `GET /departments/{id}/users` | кого можно назначить исполнителем (члены отдела) |
| `POST /tasks` | создать (тело JSON; `assignee` обязателен и из отдела; опц. `parent_id`) |
| `POST /tasks/{id}/move` | перевести (`column`, `note`) |
| `POST /tasks/{id}/comment` | комментировать |
| `POST /tasks/{id}/dispute-deadline` | оспорить дедлайн (исполнитель) |
| `POST /tasks/{id}/resolve-deadline` | разрешить оспаривание (автор/менеджер) |
| `PATCH /tasks/{id}` | правка (автор/менеджер) |
| `DELETE /tasks/{id}` | удалить (автор/менеджер) |
| `POST /types`, `POST /projects`, `POST /departments` | структура (менеджер) |

MCP-тулы и REST-маршруты — два фасада над одними методами сервиса: расхождению в правах
взяться неоткуда.

---

## Системные настройки

| Ключ | Умолчание | Смысл |
|---|---|---|
| `mxboard.default_project` | `default` | проект по умолчанию |
| `mxboard.api_enabled` | `1` | REST-эндпоинт (rest.php) включён — реальный гейт |
| `mxboard.mcp_enabled` | `1` | MCP включён |
| `mxboard.allow_self_close` | `0` | разрешить закрывать свою же задачу |
| `mxboard.wip_limit` | `0` | лимит задач в работе на исполнителя |
| `mxboard.group_admin_authority` | `0` | супер группы, если `role.authority <= порог` (меньше = больше прав; `0` = выключено) |
| `mxboard.media_source` | `0` | медиа-источник для файлов (0 = дефолтный) |

---

## Что НЕ трогаем (работает)

Журнал, сброс OPcache в деплое, поле `position` (не `rank`), гвард `hasPermission` без
сессии (`Transitions::isSuperuser`).

> ~~Атомарный захват~~ — **удалён** (решение 13): исполнитель назначается при создании,
> свободного пула нет, гонки за карточку тоже. Историческая справка, не актуально.

---

## Чек-лист

### Фаза 0 — проверить перед кодом
- [x] Механика «супер-пользователь группы» в MODX 3 (`modUserGroupMember.role` → `modUserGroupRole.authority`) — сверено по codegraph (эталон MODX 3). **Итог:** `authority` (int unsigned, default 9999), **меньше = больше прав**; `role=0` = роли нет. Супер группы = членство с `role != 0` и `role.authority <= mxboard.group_admin_authority`. Проверка — JOIN `modUserGroupMember`+`modUserGroupRole` (не `hasPermission` — сессии в API нет). Порог `0` = выключено (штатный минимум authority=1).

### Фаза 1 — фундамент (слой данных)
- [x] Переписать схему (9 таблиц)
- [x] Перегенерировать модели (`modxapp schema`) — Board удалён, добавлены Department/Project/TaskType/Field
- [x] `Transitions`: финальная=автор, запрет самозакрытия, `isManager` (sudo + супер группы через authority JOIN); блок финальной вынесен в `TaskService::move`
- [x] `TaskService`: `create` (валидация тип/title≤250/deadline/обязательные поля; опц. `parent_id`), `take`, `move` (блок финальной при открытых потомках), `comment`, `disputeDeadline`, `resolveDeadline`, `update`, `delete`
- [x] Хелпер `Visibility` (canView: author/assignee/соисполнитель подзадачи/супер-группы/sudo; boardCondition: родитель НЕ в канбане соисполнителя)
- [x] Резолвер 01 — таблицы (utf8mb4)
- [x] Резолвер 02 — сид: отдел (группа mxBoard), типы bugfix+feature с полями, проект default с колонками (инварианты), шаблон колонок (project_id=0)
- [x] Настройки обновить (default_project, group_admin_authority, media_source)
- [x] Дроп + пересоздание на стенде (`_reset_remote.php`)
- [x] Smoke слоя данных: 33/33 — валидация, оспаривание, видимость, инварианты, подзадача блокирует финальную
- [x] Коммит (715afd1)

### Фаза 2 — агентские каналы: MCP + REST
- [x] `StructureService` (общий для MCP/REST): `createType` (с полями, инвариант ≥1), `createProject` (с колонками/шаблоном, инвариант одна initial/final), `registerDepartment`; охват — менеджер
- [x] `BoardQuery` (общий): `projects`, `board` (видимость), `taskDetail` (canView), `typeSchema`
- [x] `Server` (MCP): `tools/list` фильтруется (структурные тулы — только менеджеру); тулы всех уровней
- [x] `PasswordAuth` (Basic через `passwordMatches`) + `ApiAuth` (Bearer→Basic), подключены в `mcp.php` и `rest.php` (оба метода на обоих каналах)
- [x] `rest.php` + `Rest\Router`: маршруты (PATH_INFO + фолбэк `?path=`), авторизация `ApiAuth`, гейт `api_enabled`, канал `api`
- [x] Менеджер через authority-JOIN: фикс алиаса связи `UserGroupRole` (был `Role` — join не строился)
- [x] Фикс дедлайна: разбор даты-строки централизован в `TaskService::normalizeDeadline` (REST обрезал "2026-12-31" в 2026)
- [x] Smoke in-process 16/16: фильтрация tools/list, паритет прав MCP↔REST, менеджер через группу-супер
- [x] curl-smoke: обе авторизации (Bearer/Basic) на обоих каналах, 401 без прав, PATH_INFO и `?path`, lifecycle create/get/delete, дата-дедлайн
- [x] Коммит (c38918e)

### Фаза 3 — процессоры + UI (подфазы)

**Карта UI (зафиксировано):** канбан с селектором **отдела + проекта** (видишь любой; доска
пуста, где не участвуешь); **задача — отдельная страница** (вью через ручной switch, без
vue-router; навигация родитель↔подзадача, «← на доску»); **Структура — один экран с
вкладками** (Отделы · Типы+поля · Проекты · Колонки/стадии), только менеджер; Токены —
существующий раздел.

#### 3a — Процессоры (бэкенд UI, без браузера) ✅
- [x] `StructureService` дополнен CRUD: update/remove для department/type/project + field
  create/update/remove + column create/update/remove. Инварианты при удалении (последнее
  поле типа, initial/final-колонка, непустые сущности), перенос флага initial/final через update.
- [x] Процессоры-зеркала сервисов через базовый `ServiceProcessor`: `Board/Get` (scoped +
  видимость), списки (Department GetList/Users, Project/GetList, Type GetList/Schema, Column/GetList,
  Field/GetList), `Task` CRUD (Create с исполнителем/типом/дедлайном/подзадачей, Get с журналом,
  Move, Comment, Update, Remove, DisputeDeadline, ResolveDeadline), структура (Department/Type/
  Field/Project/Column Create/Update/Remove — менеджер). Удалён v1 `Task/GetList`.
- [x] Роутинг коннектора: `action`=FQCN процессора (без изменений, как в v1). Новые ключи лексикона ru+en.
- [x] Smoke процессоров: **54/54** (in-process через `$modx->runProcessor`, `_proc_smoke_remote.php`).
  Права/видимость/инварианты/переходы. Не curl-через-connector: авторизация коннектора (context
  ACL + токен) — код ядра, проверенный рабочим UI v1; здесь тестируем связку процессор→сервис.
- [x] Коммит (b300336).

#### 3b — UI ядро: канбан + страница задачи
- [x] Весь фронт переведён на **VueTools `@vuetools/useApi`** (эталон: раньше был свой fetch,
  как в mxlogger3 — техдолг). Адаптер в `connector.js`: useApi с нашими `baseUrl`/`token`,
  `X-Requested-With`; API-слой пробрасывает промисы, компоненты — try/catch. Мигрирован и
  `TokensView`. Бандл: три external-импорта (`vue`, `primevue`, `@vuetools/useApi`), 38.8 КБ.
- [x] Селекторы отдел+проект (Select); доска с видимостью; фильтр Все/Я-автор/Я-исполнитель.
  Карточка: тип, исполнитель, дедлайн (просрочка/оспаривание), значок подзадачи. Drag&drop по `column.key`.
- [x] Страница задачи (`TaskPage`, ручной switch без vue-router): динамическая форма по типу
  (`TypeFields`), дедлайн+оспаривание/решение, подзадачи (список/создание/навигация
  родитель↔подзадача), комментарии, журнал, правка, смена стадии, выбор исполнителя из отдела, «← на доску».
- [x] `NewTaskDialog`: тип→схема→динамические поля, дедлайн, исполнитель из отдела, `parent_id`.
- [x] Пересборка бандла + деплой на стенд. `@vuetools/useApi` подтверждён в Import Map стенда (VueTools 1.1.2-pl).
- [x] Коммит (06b3ee9).
- [x] **Лексиконы: весь UI вынесен в словари** (правило пользователя — хардкод строк запрещён).
  `useLexicon` (VueTools) + `utils/i18n.js`; ключи `mxboard_ui_*`/`mxboard_msg_*`/`mxboard_act_*`
  в ru+en (204 шт.); контроллер CMP прокидывает `fetch('mxboard')` в `window.MODx.lang`.
  Бандл: 4 external (`vue`, `primevue`, `@vuetools/useApi`, `@vuetools/useLexicon`).
- [x] **Браузер-проверка монтирования — сделана автономно** (playwright + локальный harness:
  скачанная публичная статика VueTools + наш бандл, `file://`/localhost, без прода и без
  сессии). Результат: смонтировано, 203 ключа в MODx.lang, вкладки/доска на реальных строках,
  ноль сырых ключей, консоль чистая, все 4 модуля резолвятся. Живой прод-рендер — за пользователем утром.

#### 3c — UI Структура (вкладки) + фильтры
- [ ] Экран «Структура» с вкладками: Отделы (реестр — пометить группу), Типы+поля,
  Проекты, Колонки/стадии (в редакторе проекта) + шаблон. Гейт «менеджер».
- [ ] Пересборка, браузер. Коммит.

#### 3d — Кнопка + поле «Токен агента» в профиле пользователя
- [ ] Плагин на `OnManagerPageBeforeRender` (детект security/user/update|create), гейт
  `sudo`, инъекция JS (`addLastJavascript`/`addHtml`). Поле показывает текущий токен, кнопка
  генерирует/перевыпускает.
- [ ] Хранение (вариант A): сырой токен в `profile.extended.mxboard` (`token`, `token_id`,
  `createdon`) — виден/копируется; `sha256` — в `mxboard_token` для входа. Один токен на
  пользователя: генерация удаляет прежнюю строку по `token_id` (именованные токены старого
  раздела не трогает). Процессор `Token/IssueForUser`.
- [ ] Браузер. Коммит.

#### 3e — Уведомления + непрочитанные комментарии  ⛔ ЖДЁТ порт `universaleventbus` на MODX 3
> Транспорт доставки — `universaleventbus` (пакет пользователя), сейчас MODX 2. Порт —
> **отдельная задача**; 3e начинается после него. Часть без транспорта (таблицы, событие,
> плагин, непрочитанные-бейдж по данным) можно сделать раньше, live-доставку — после порта.
- [ ] (порт universaleventbus на MODX 3 — отдельная задача, вне mxBoard)
- [ ] Схема +2 таблицы: `mxboard_notification` (`user_id`, `task_id`, `type`, `read`,
  `createdon`), `mxboard_task_seen` (`user_id`, `task_id`, `seenon`). Регенерация, резолвер, реинсталл.
- [ ] Событие `mxbOnTaskAssign` (create + переназначение) в `TaskService`; зарегистрировать.
- [ ] Бандл-плагин `mxBoardNotify` на события → пишет `mxboard_notification` + кладёт в очередь
  `universaleventbus` (`branch = mxb_user_<id>`) по правилам получателей (назначена→исполнитель;
  стадия→вторая сторона; выполнена→автор; комментарий→автор+исполнитель+участники подзадач, кроме инициатора).
- [ ] Процессоры: список уведомлений, отметить прочитанным, `seen` (сброс `seenon` при открытии задачи).
- [ ] UI: колокольчик со списком (+ live через `eventbus.js`); бейдж непрочитанных комментариев
  на карточке канбана (`createdon > seenon`).
- [ ] Браузер, проверка live-доставки. Коммит.

### Фаза 4 — документация
- [ ] `docs/readme.txt`, `README.md` — обновить под v2
- [ ] БЗ: `modx-modules/mxboard`, `tasks/` запись
- [ ] Финальный smoke + скриншоты
