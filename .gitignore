/core/packages/
/core/components/mxboard/vendor/
/assets/components/mxboard/node_modules/
/assets/components/mxboard/js/mgr/node_modules/
/.idea/
.DS_Store
/config/
/refs/

# impeccable (локальное состояние ревьюера)
.impeccable/

# Разовый проверочный скрипт пагинации обзора (карточка #2607-107): проверял
# конкретный фикс на стенде, в поставку не входит и повторного применения не имеет.
/_overview_paging_remote.php

# Разовый смоук свободного поиска задач (карточка #2607-130): проверял конкретную
# правку на стенде (SQL-форма условия, три сценария, скоуп видимости), в поставку
# не входит.
/_search_smoke_remote.php

# Разовая диагностика членства пользователей в группах отделов (карточка #2607-132):
# отвечала на вопрос «не отрежет ли новая ACL-проверка действующих авторов», ответ
# получен (нарушений 0) и повторного применения не имеет.
/_members_check_remote.php

# Разовая инвентаризация scope колонок (карточка #2607-217): считала, у каких проектов
# есть собственные колонки и сколько карточек ссылается на чужой scope. Ответ получен,
# повторяемая часть живёт в _column_scope_repair_remote.php (dry-run).
/_column_scope_audit.php
